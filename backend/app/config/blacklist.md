# Blacklist Targets

<!-- 
Add blocked domains, IPs, or patterns below.
Format:
- example.com          (exact match + subdomains)
- *.example.org        (wildcard - all subdomains)
- 192.168.1.0/24       (IP range)

Note: .gov and .mil TLDs are always blocked by default.
-->

